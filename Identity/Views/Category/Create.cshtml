@model Identity.Models.Category

@{
    ViewData["Title"] = "Create";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<section class="bg-light py-3 py-md-5">
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-12 col-sm-10 col-md-8 col-lg-6 col-xl-5 col-xxl-4">
                <div class="card border border-light-subtle rounded-3 shadow-sm">
                    <div class="card-body p-3 p-md-4 p-xl-5">
                        <h4 class="text-center">Create New Category</h4><hr />
                        <form asp-action="Create">
                            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                            <div class="row gy-2 overflow-hidden">
                                <div class="col-12">
                                    <div class="form-floating">
                                        <input asp-for="Name" class="form-control" id="Name" placeholder="Category Name">
                                        <label asp-for="Name" class="form-label">Category Name</label>
                                        <span asp-validation-for="Name" class="text-danger"></span>
                                    </div>
                                </div>
                                <div class="col-12 d-flex gap-1">
                                    <div class="d-grid my-3 col-6">
                                        <button class="btn btn-success btn-lg" type="submit">Create</button>
                                    </div>
                                    <div class="d-grid my-3 col-6">
                                        <a class="btn btn-danger btn-lg" asp-action="Index">Cancle</a>
                                    </div>
                                </div>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>
@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
